import React from "react";
import { View, Text, StyleSheet } from "react-native";

const QnA = (props) => {
  return (
    <View>
      <Text>Q&A</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default QnA;
